# Поддержка нескольких бирж через CCXT

Торговый бот теперь поддерживает торговлю на множестве криптовалютных бирж через библиотеку CCXT (CryptoCurrency eXchange Trading Library).

## 🌐 Поддерживаемые биржи

CCXT поддерживает **100+ криптовалютных бирж**, включая:

### Основные биржи:
- **Binance** - крупнейшая криптобиржа
- **Coinbase** / **Coinbase Pro** - популярная биржа США
- **Kraken** - старая и надежная биржа
- **Bybit** - деривативы и споты
- **OKX** - крупная биржа
- **Huobi** - азиатская биржа
- **KuCoin** - разнообразные токены
- **Bitfinex** - профессиональная торговля
- **Gate.io** - большой выбор альткоинов
- **Bittrex** - американская биржа

### Полный список:
Смотрите на GitHub CCXT: https://github.com/ccxt/ccxt#supported-cryptocurrency-exchange-markets

---

## 📋 Режимы работы

### Режим 1: Legacy Binance (по умолчанию)
Использует оригинальную библиотеку `python-binance` для работы только с Binance.

**Конфигурация:**
```bash
USE_CCXT=false
BINANCE_API_KEY=ваш_ключ
BINANCE_API_SECRET=ваш_секрет
BINANCE_TESTNET=false
```

**Преимущества:**
- Проверенный код
- Полная обратная совместимость
- Специфичные функции Binance

### Режим 2: CCXT Multi-Exchange (новый)
Использует CCXT для подключения к любой поддерживаемой бирже.

**Конфигурация:**
```bash
USE_CCXT=true
EXCHANGE_ID=binance  # или kraken, coinbase, bybit и т.д.
EXCHANGE_API_KEY=ваш_ключ
EXCHANGE_API_SECRET=ваш_секрет
EXCHANGE_TESTNET=false
```

**Преимущества:**
- Поддержка 100+ бирж
- Унифицированный API
- Легкое переключение между биржами
- Возможность торговли на нескольких биржах одновременно

---

## 🚀 Быстрый старт

### Шаг 1: Обновите requirements.txt

Библиотека CCXT уже добавлена в `requirements.txt`:
```bash
ccxt>=4.2.0
```

Установите зависимости:
```bash
pip install -r requirements.txt
# или в virtual environment:
/opt/trading-bot/venv/bin/pip install -r requirements.txt
```

### Шаг 2: Получите API ключи

Создайте API ключи на выбранной бирже:

#### Binance:
1. Зайдите на https://www.binance.com/
2. API Management → Create API
3. Включите "Enable Spot & Margin Trading"
4. **НЕ включайте** "Enable Withdrawals"

#### Kraken:
1. Зайдите на https://www.kraken.com/
2. Settings → API → Generate New Key
3. Включите "Create & Modify Orders"
4. Включите "Query Funds"

#### Coinbase Pro:
1. Зайдите на https://pro.coinbase.com/
2. API → New API Key
3. Permissions: View, Trade

### Шаг 3: Настройте .env файл

Для Binance через CCXT:
```bash
USE_CCXT=true
EXCHANGE_ID=binance
EXCHANGE_API_KEY=ваш_binance_api_key
EXCHANGE_API_SECRET=ваш_binance_api_secret
EXCHANGE_TESTNET=false
```

Для Kraken:
```bash
USE_CCXT=true
EXCHANGE_ID=kraken
EXCHANGE_API_KEY=ваш_kraken_api_key
EXCHANGE_API_SECRET=ваш_kraken_api_secret
EXCHANGE_TESTNET=false
```

Для Coinbase Pro:
```bash
USE_CCXT=true
EXCHANGE_ID=coinbasepro
EXCHANGE_API_KEY=ваш_coinbase_api_key
EXCHANGE_API_SECRET=ваш_coinbase_api_secret
EXCHANGE_TESTNET=false
```

### Шаг 4: Обновите символ торговли

Каждая биржа использует свой формат символов:

**Binance:** `BTC/USDT`  
**Kraken:** `BTC/USD` или `XBT/USD`  
**Coinbase:** `BTC/USD`  
**Bybit:** `BTC/USDT`

В .env файле:
```bash
DEFAULT_SYMBOL=BTC/USDT  # для большинства бирж
# или
DEFAULT_SYMBOL=BTC/USD   # для Kraken, Coinbase
```

### Шаг 5: Запустите бота

```bash
sudo systemctl restart trading-bot
sudo journalctl -u trading-bot -f
```

В логах вы увидите:
```
Connected to binance PRODUCTION (CCXT)
# или
Connected to kraken PRODUCTION (CCXT)
```

---

## 🔄 Переключение между биржами

Чтобы переключиться на другую биржу:

1. Остановите бота:
```bash
sudo systemctl stop trading-bot
```

2. Отредактируйте конфигурацию:
```bash
sudo nano /etc/trading-bot/.env
```

3. Измените параметры:
```bash
EXCHANGE_ID=kraken  # новая биржа
EXCHANGE_API_KEY=ключ_от_kraken
EXCHANGE_API_SECRET=секрет_от_kraken
DEFAULT_SYMBOL=BTC/USD  # формат символа для Kraken
```

4. Перезапустите бота:
```bash
sudo systemctl start trading-bot
```

---

## 📊 Примеры конфигурации для разных бирж

### Binance
```bash
USE_CCXT=true
EXCHANGE_ID=binance
EXCHANGE_API_KEY=ваш_ключ
EXCHANGE_API_SECRET=ваш_секрет
EXCHANGE_TESTNET=false
DEFAULT_SYMBOL=BTC/USDT
```

### Kraken
```bash
USE_CCXT=true
EXCHANGE_ID=kraken
EXCHANGE_API_KEY=ваш_ключ
EXCHANGE_API_SECRET=ваш_секрет
EXCHANGE_TESTNET=false
DEFAULT_SYMBOL=BTC/USD
```

### Coinbase Pro
```bash
USE_CCXT=true
EXCHANGE_ID=coinbasepro
EXCHANGE_API_KEY=ваш_ключ
EXCHANGE_API_SECRET=ваш_секрет
EXCHANGE_TESTNET=false
DEFAULT_SYMBOL=BTC/USD
```

### Bybit
```bash
USE_CCXT=true
EXCHANGE_ID=bybit
EXCHANGE_API_KEY=ваш_ключ
EXCHANGE_API_SECRET=ваш_секрет
EXCHANGE_TESTNET=true  # Bybit поддерживает testnet!
DEFAULT_SYMBOL=BTC/USDT
```

### OKX
```bash
USE_CCXT=true
EXCHANGE_ID=okx
EXCHANGE_API_KEY=ваш_ключ
EXCHANGE_API_SECRET=ваш_секрет
EXCHANGE_TESTNET=true  # OKX поддерживает testnet!
DEFAULT_SYMBOL=BTC/USDT
```

### KuCoin
```bash
USE_CCXT=true
EXCHANGE_ID=kucoin
EXCHANGE_API_KEY=ваш_ключ
EXCHANGE_API_SECRET=ваш_секрет
EXCHANGE_TESTNET=false
DEFAULT_SYMBOL=BTC/USDT
```

---

## 🧪 Тестирование на Testnet

Некоторые биржи поддерживают тестовые сети (testnet):

**Поддерживают testnet:**
- Binance (testnet.binance.vision)
- Bybit (testnet.bybit.com)
- OKX (testnet.okx.com)

**Не поддерживают:**
- Kraken (нет testnet)
- Coinbase (нет testnet)
- Большинство других бирж

Для тестирования на биржах без testnet:
1. Создайте новый аккаунт
2. Внесите минимальную сумму
3. Установите минимальные размеры позиций

---

## 🔍 Проверка поддерживаемых символов

Узнайте, какие торговые пары поддерживает биржа:

```python
import ccxt

# Создайте экземпляр биржи
exchange = ccxt.binance()
exchange.load_markets()

# Все символы
print(exchange.symbols)

# Поиск BTC пар
btc_pairs = [s for s in exchange.symbols if 'BTC' in s]
print(btc_pairs)
```

Или используйте веб-интерфейс биржи для просмотра доступных пар.

---

## ⚙️ Технические детали

### Архитектура

Добавлен новый модуль `ExchangeAdapter` (`src/core/exchange_adapter.py`), который:

1. **Унифицирует API** - предоставляет единый интерфейс для всех бирж
2. **Обратная совместимость** - поддерживает legacy Binance клиент
3. **Автоматическое преобразование** - конвертирует форматы данных между биржами

### Основные методы:

```python
exchange = ExchangeAdapter(config)

# Получить тикер
ticker = exchange.get_symbol_ticker('BTC/USDT')

# Получить свечи
klines = exchange.get_klines('BTC/USDT', '1h', 100)

# Создать ордер
order = exchange.create_order('BTC/USDT', 'buy', 'market', 0.001)

# Получить баланс
account = exchange.get_account()

# Проверить подключение
exchange.ping()
```

### Стратегии

Все существующие стратегии работают без изменений благодаря `ExchangeAdapter`, который преобразует данные в унифицированный формат.

---

## 🐛 Устранение неполадок

### Проблема: "Exchange not found"

**Решение:** Проверьте правильность `EXCHANGE_ID`. Список доступных: https://github.com/ccxt/ccxt#supported-cryptocurrency-exchange-markets

### Проблема: "Invalid API credentials"

**Решение:**
1. Проверьте правильность API ключей
2. Убедитесь, что API ключ имеет необходимые разрешения
3. Проверьте, что IP адрес сервера в whitelist (если настроен)

### Проблема: "Symbol not found"

**Решение:**
1. Проверьте формат символа для конкретной биржи
2. Binance: `BTC/USDT`
3. Kraken: `BTC/USD` или `XBT/USD`
4. Используйте `exchange.load_markets()` для просмотра доступных пар

### Проблема: "Rate limit exceeded"

**Решение:**
- CCXT автоматически управляет rate limits
- Если проблема persist, увеличьте интервал проверки в `bot.py`
- Измените `time.sleep(60)` на большее значение

### Проблема: "Testnet not supported"

**Решение:**
- Не все биржи поддерживают testnet
- Используйте `EXCHANGE_TESTNET=false`
- Тестируйте с минимальными суммами на основной сети

---

## 📚 Дополнительные ресурсы

- **CCXT Documentation:** https://docs.ccxt.com/
- **CCXT GitHub:** https://github.com/ccxt/ccxt
- **Supported Exchanges:** https://github.com/ccxt/ccxt#supported-cryptocurrency-exchange-markets
- **CCXT Manual:** https://docs.ccxt.com/en/latest/manual.html

---

## ⚠️ Важные замечания

1. **Тестируйте сначала на testnet** (если доступен) или с минимальными суммами
2. **Каждая биржа имеет свои комиссии** - учитывайте это в стратегии
3. **Форматы символов различаются** - проверяйте правильный формат для биржи
4. **Rate limits различаются** - некоторые биржи более строгие
5. **API ключи должны иметь только торговые права** - НЕ давайте права на вывод средств
6. **Храните ключи в безопасности** - файл `.env` должен иметь права 600

---

## 🎯 Следующие шаги

1. ✅ Установите CCXT: `pip install ccxt`
2. ✅ Получите API ключи на выбранной бирже
3. ✅ Настройте `.env` с `USE_CCXT=true`
4. ✅ Укажите `EXCHANGE_ID` и `DEFAULT_SYMBOL`
5. ✅ Запустите бота в режиме мониторинга (`TRADING_ENABLED=false`)
6. ✅ Проверьте логи и подключение
7. ✅ Протестируйте на testnet или с минимальными суммами
8. ✅ Запустите реальную торговлю

**Удачной торговли на множестве бирж! 🚀**
