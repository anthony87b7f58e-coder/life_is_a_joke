# Исправление проблем с ограничениями и уведомлениями

## 🐛 Исправленные проблемы

### 1. Бот игнорировал ограничения MAX_POSITIONS и MAX_DAILY_TRADES
**Проблема**: Бот открывал неограниченное количество позиций, не проверяя лимиты.

**Решение**: Добавлены явные проверки лимитов в НАЧАЛЕ методов `_execute_buy()` и `_execute_sell()`:
```python
# CHECK RISK LIMITS FIRST - CRITICAL!
if not self.risk_manager.check_daily_limits():
    self.logger.warning(f"Skipping BUY {symbol}: Daily limits reached")
    return

if not self.risk_manager.check_position_limits():
    self.logger.warning(f"Skipping BUY {symbol}: Position limits reached")
    return
```

Теперь ПЕРЕД открытием каждой позиции проверяется:
- ✅ Не превышен ли лимит дневных сделок (`MAX_DAILY_TRADES`)
- ✅ Не превышен ли лимит открытых позиций (`MAX_OPEN_POSITIONS`)
- ✅ Не превышен ли лимит дневных убытков (`MAX_DAILY_LOSS_PERCENTAGE`)

### 2. Ошибка Telegram "could not convert string to float: 'None'"
**Проблема**: При отправке уведомлений об открытой позиции возникала ошибка конвертации.

**Решение**: Добавлена изоляция уведомлений в собственный try-except блок:
```python
# Send Telegram notification - wrap in try-except to prevent exceptions
try:
    notifier = get_notifier()
    if notifier:
        notifier.notify_position_opened(
            symbol=symbol,
            side='BUY',
            quantity=quantity,
            price=price,
            strategy=strategy.name,
            score=score
        )
except Exception as notif_error:
    self.logger.error(f"Failed to send notification: {notif_error}", exc_info=True)
```

Теперь:
- ✅ Ошибка в уведомлении НЕ прерывает торговлю
- ✅ Ошибка логируется, но не вызывает отправку ошибочного уведомления
- ✅ Позиции открываются успешно, даже если уведомление не отправляется

### 3. Автоматическое закрытие позиций
**Статус**: Функциональность ЕСТЬ, но нужно проверить работу стратегий.

Стратегии генерируют сигналы CLOSE когда:
- Условия для закрытия выполнены
- Stop-loss достигнут (анализируется стратегией)
- Take-profit достигнут (анализируется стратегией)

## 📋 Что изменилось в коде

### src/strategies/strategy_manager.py

**В методе `_execute_buy()`**:
1. Добавлены проверки лимитов В НАЧАЛЕ метода (до любых вычислений)
2. Обёрнут вызов `notify_position_opened()` в try-except
3. Обёрнут вызов `notify_error()` в try-except

**В методе `_execute_sell()`**:
1. Добавлены проверки лимитов В НАЧАЛЕ метода
2. Обёрнут вызов `notify_position_opened()` в try-except
3. Обёрнут вызов `notify_error()` в try-except

## 🔍 Как проверить, что всё работает

### Проверка лимитов

После обновления в логах должно быть:

**При достижении лимита позиций**:
```
WARNING - Skipping BUY BTCUSDT: Position limits reached
```

**При достижении лимита дневных сделок**:
```
WARNING - Skipping BUY ETHUSDT: Daily limits reached
```

**При работающих лимитах**:
```
INFO - Signal from EnhancedMultiIndicator: BUY BTCUSDT at 99000.0
INFO - Executing BUY: 0.001 BTCUSDT at 99000.0 (SL: 97020.0, TP: 103950.0)
```

### Проверка уведомлений

**Успешное уведомление**:
```
INFO - Position opened: ID 123
# Уведомление приходит в Telegram без ошибок
```

**Ошибка уведомления (безопасная)**:
```
INFO - Position opened: ID 123
ERROR - Failed to send position opened notification: <error details>
# Позиция ОТКРЫТА, но уведомление не отправлено
# Ошибочное уведомление НЕ ОТПРАВЛЯЕТСЯ
```

## 🚀 Обновление на сервере

```bash
# 1. Остановите бота
sudo systemctl stop trading-bot

# 2. Найдите и перейдите в директорию проекта
cd $(find ~ -name "life_is_a_joke" -type d 2>/dev/null | head -1)

# 3. Обновите код
git pull origin copilot/merge-all-branches

# 4. Проверьте, что обновление применилось
git log --oneline -1
# Должен быть коммит с исправлением лимитов

# 5. Проверьте файл
grep -A 5 "CHECK RISK LIMITS FIRST" src/strategies/strategy_manager.py
# Должны увидеть новые проверки

# 6. Запустите бота
sudo systemctl start trading-bot

# 7. Проверьте логи
sudo journalctl -u trading-bot -f
```

## ⚙️ Настройка лимитов

В файле `.env` или переменных окружения:

```bash
# Максимум открытых позиций одновременно
MAX_OPEN_POSITIONS=20

# Максимум сделок в день
MAX_DAILY_TRADES=50

# Максимальный дневной убыток (%)
MAX_DAILY_LOSS_PERCENTAGE=5.0

# Размер позиции как % от баланса
POSITION_SIZE_PERCENTAGE=2.0

# Максимальный размер одной позиции
MAX_POSITION_SIZE=0.1
```

**Проверка текущих настроек**:
```bash
# На сервере
cat .env | grep -E "MAX_OPEN_POSITIONS|MAX_DAILY_TRADES"

# Или из логов при запуске
sudo journalctl -u trading-bot --since "5 minutes ago" | grep "Risk Manager initialized"
# Должно показать: Risk Manager initialized - Max daily trades: 50, Max daily loss: 5.0%
```

## 🔧 Диагностика проблем

### Бот всё ещё открывает слишком много позиций

1. **Проверьте обновление кода**:
   ```bash
   cd /путь/к/life_is_a_joke
   grep -c "CHECK RISK LIMITS FIRST" src/strategies/strategy_manager.py
   ```
   Должно вывести: `2` (для BUY и SELL)

2. **Проверьте логи на наличие предупреждений**:
   ```bash
   sudo journalctl -u trading-bot --since "1 hour ago" | grep -i "limits reached"
   ```

3. **Проверьте конфигурацию**:
   ```bash
   env | grep MAX_OPEN_POSITIONS
   env | grep MAX_DAILY_TRADES
   ```

### Уведомления Telegram всё ещё с ошибками

1. **Проверьте версию notifications.py**:
   ```bash
   grep -c "except Exception as notif_error" src/strategies/strategy_manager.py
   ```
   Должно вывести: `4` (по 2 в BUY и SELL)

2. **Проверьте логи на ошибки уведомлений**:
   ```bash
   sudo journalctl -u trading-bot -f | grep -i "notification"
   ```

3. **Если ошибка всё ещё появляется** - значит сервер не обновлён:
   ```bash
   git log --oneline -3
   # Должны видеть коммит с "Wrap notifications in try-except"
   ```

### Позиции не закрываются автоматически

1. **Проверьте, генерируют ли стратегии сигналы CLOSE**:
   ```bash
   sudo journalctl -u trading-bot --since "1 day ago" | grep "CLOSE"
   ```

2. **Проверьте открытые позиции в базе данных**:
   ```bash
   sqlite3 /var/lib/trading-bot/trading_bot.db "SELECT * FROM positions WHERE status='open';"
   ```

3. **Убедитесь, что стратегия анализирует открытые позиции**:
   ```bash
   sudo journalctl -u trading-bot -f | grep -i "position"
   ```

## ✅ Контрольный список

После обновления проверьте:

- [ ] Код обновлён (git log показывает новый коммит)
- [ ] Новые проверки присутствуют в файле (grep показывает "CHECK RISK LIMITS FIRST")
- [ ] Бот перезапущен
- [ ] В логах видны проверки лимитов
- [ ] При достижении лимита появляется WARNING
- [ ] Позиции открываются только в пределах лимитов
- [ ] Уведомления Telegram приходят без ошибок
- [ ] Если ошибка уведомления - она только логируется, не отправляется

## 📊 Ожидаемое поведение

### Нормальная работа:
```
INFO - Signal from EnhancedMultiIndicator: BUY ADAUSDT at 0.397
INFO - Available currencies: ['USDT', 'ADA', 'BTC']
INFO - Non-zero balances: {'USDT': 4520.8}
INFO - USDT balance from balance['free']: 4520.8
INFO - Calculated position size: 0.01
INFO - Executing BUY: 0.01 ADAUSDT at 0.397
INFO - BUY order executed: Order ID 12345
INFO - Position opened: ID 67
# Telegram: ✅ Уведомление об открытой позиции приходит
```

### При достижении лимитов:
```
INFO - Signal from EnhancedMultiIndicator: BUY SOLUSDT at 150.0
WARNING - Skipping BUY SOLUSDT: Position limits reached
# Ордер НЕ размещается
# Уведомление НЕ отправляется
```

### При ошибке уведомления (но позиция открыта):
```
INFO - BUY order executed: Order ID 12345
INFO - Position opened: ID 67
ERROR - Failed to send position opened notification: <details>
# Позиция ОТКРЫТА
# Ошибочное уведомление НЕ отправляется
# Бот продолжает работать
```

---

**Версия**: 2.1  
**Дата**: 2026-01-09  
**Коммит**: См. последний коммит в ветке copilot/merge-all-branches
