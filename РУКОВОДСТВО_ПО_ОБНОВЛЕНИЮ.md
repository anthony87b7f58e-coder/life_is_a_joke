# Руководство по обновлению кода на сервере

## 🐛 Исправленная ошибка

**Проблема**: Бот не мог найти баланс USDT в аккаунте и выдавал ошибку:
```
WARNING - Could not find USDT in expected locations. Balance keys: ['info', 'timestamp', 'datetime', 'free', 'used', 'total']
```

**Причина**: Код неправильно обращался к структуре данных баланса от CCXT. Вместо проверки наличия ключа `'USDT'` в словаре, он пытался получить доступ к несуществующему ключу.

**Решение**: Исправлена логика получения баланса USDT с использованием метода `.get()`, который возвращает 0, если ключ не найден.

## 📋 Измененные файлы

- `src/strategies/strategy_manager.py` - исправлена логика получения баланса USDT

## 🚀 Шаг 1: Остановка бота на сервере

Сначала нужно остановить работающий бот:

```bash
# Если бот запущен как systemd сервис:
sudo systemctl stop trading-bot

# Или если запущен вручную, найдите и остановите процесс:
ps aux | grep "python.*main.py"
# Найдите PID (число во втором столбце) и выполните:
kill <PID>
```

## 🚀 Шаг 2: Подключение к серверу и переход в директорию проекта

```bash
# Подключитесь к серверу по SSH
ssh username@your-server-ip

# Перейдите в директорию проекта
cd /path/to/life_is_a_joke

# Проверьте текущую ветку
git branch
# Должно показать: * copilot/merge-all-branches
```

## 🚀 Шаг 3: Получение обновлений из GitHub

```bash
# Сохраните локальные изменения (если есть)
git stash

# Получите последние изменения
git fetch origin copilot/merge-all-branches

# Переключитесь на последнюю версию
git pull origin copilot/merge-all-branches

# Или если нужно жесткое обновление (удалит локальные изменения):
# git reset --hard origin/copilot/merge-all-branches
```

## 🚀 Шаг 4: Проверка обновлений

```bash
# Проверьте, что файл обновлен
git log --oneline -5

# Вы должны увидеть коммит с исправлением баланса
# Посмотрите изменения в файле
cat src/strategies/strategy_manager.py | grep -A 10 "balance\['free'\]\.get"
```

## 🚀 Шаг 5: Обновление зависимостей (если нужно)

```bash
# Активируйте виртуальное окружение (если используете)
source venv/bin/activate

# Обновите зависимости Python (на всякий случай)
pip install -r requirements.txt
```

## 🚀 Шаг 6: Запуск обновленного бота

### Вариант А: Запуск через systemd (рекомендуется)

```bash
# Запустите сервис
sudo systemctl start trading-bot

# Проверьте статус
sudo systemctl status trading-bot

# Посмотрите логи
sudo journalctl -u trading-bot -f

# Или с фильтром по времени
sudo journalctl -u trading-bot --since "5 minutes ago"
```

### Вариант Б: Запуск вручную

```bash
# В виртуальном окружении
source venv/bin/activate
python src/main.py

# Или в фоновом режиме
nohup python src/main.py > bot.log 2>&1 &

# Посмотреть логи
tail -f bot.log
```

## 🔍 Шаг 7: Проверка работы

После запуска проверьте логи. Теперь вы должны видеть:

```
✅ ПРАВИЛЬНО:
INFO - Available USDT balance: $XX.XX
DEBUG - USDT balance from balance['free']: XX.XX
DEBUG - Available currencies in balance['free']: ['USDT', ...]

❌ РАНЬШЕ БЫЛО:
WARNING - Could not find USDT in expected locations
WARNING - Balance is 0 or unavailable
```

## 📊 Шаг 8: Мониторинг

Следите за логами в течение нескольких минут, чтобы убедиться, что бот:
1. Успешно получает баланс USDT
2. Не выдает предупреждений о балансе
3. Правильно рассчитывает размер позиции на основе баланса

```bash
# Мониторинг логов в реальном времени
sudo journalctl -u trading-bot -f | grep -E "balance|USDT|position"
```

## 🛠️ Альтернативный метод: Обновление без Git

Если по какой-то причине Git не работает, можно обновить файл вручную:

```bash
# 1. Создайте резервную копию
cp src/strategies/strategy_manager.py src/strategies/strategy_manager.py.backup

# 2. Скачайте обновленный файл с GitHub
wget -O src/strategies/strategy_manager.py \
  https://raw.githubusercontent.com/anthony87b7f58e-coder/life_is_a_joke/copilot/merge-all-branches/src/strategies/strategy_manager.py

# 3. Проверьте, что файл скачался
ls -la src/strategies/strategy_manager.py

# 4. Запустите бота
sudo systemctl restart trading-bot
```

## ❓ Часто задаваемые вопросы

### Q: Нужно ли останавливать бота перед обновлением?
**A**: Да, обязательно! Иначе могут возникнуть конфликты и непредсказуемое поведение.

### Q: Потеряю ли я открытые позиции?
**A**: Нет. Позиции хранятся в базе данных и на бирже. После перезапуска бот восстановит информацию о них.

### Q: Что делать, если после обновления бот не запускается?
**A**: 
1. Проверьте логи: `sudo journalctl -u trading-bot -n 50`
2. Проверьте конфигурацию: `cat config.yaml`
3. Восстановите из резервной копии: `cp src/strategies/strategy_manager.py.backup src/strategies/strategy_manager.py`

### Q: Как проверить, что обновление применилось?
**A**: Запустите:
```bash
grep "balance\['free'\]\.get" src/strategies/strategy_manager.py
```
Если команда возвращает результат, значит обновление применено.

## 🎯 Краткая инструкция (для опытных)

```bash
# Быстрое обновление в одну команду
sudo systemctl stop trading-bot && \
cd /path/to/life_is_a_joke && \
git pull origin copilot/merge-all-branches && \
sudo systemctl start trading-bot && \
sudo journalctl -u trading-bot -f
```

## ⚠️ Важно!

1. **Всегда делайте резервную копию** перед обновлением
2. **Проверяйте логи** после обновления минимум 5-10 минут
3. **Не обновляйте во время активной торговли** - дождитесь спокойного периода
4. **Убедитесь, что есть интернет** на сервере перед обновлением

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `sudo journalctl -u trading-bot -n 100`
2. Проверьте конфигурацию: `cat config.yaml`
3. Проверьте API ключи и подключение к бирже

## ✅ Контрольный чек-лист

- [ ] Остановлен бот
- [ ] Создана резервная копия
- [ ] Получены обновления из GitHub
- [ ] Проверены изменения в файле
- [ ] Запущен обновленный бот
- [ ] Проверены логи (нет ошибок о балансе)
- [ ] Бот успешно получает баланс USDT
- [ ] Мониторинг работы 5-10 минут

---

**Дата обновления**: 2026-01-08  
**Версия**: 1.0  
**Исправленная ошибка**: Получение баланса USDT через CCXT
