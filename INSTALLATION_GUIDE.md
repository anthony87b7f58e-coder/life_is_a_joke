# Полное руководство по установке Trading Bot на сервер

Это пошаговое руководство охватывает весь процесс установки торгового бота на сервер Ubuntu/Debian от начала до конца.

## 📋 Предварительные требования

### Минимальные требования к серверу:
- **OS**: Ubuntu 20.04 LTS или новее (или Debian 11+)
- **CPU**: 2+ ядра
- **RAM**: 4+ GB
- **Disk**: 20+ GB SSD
- **Network**: Стабильное интернет-соединение
- **Root доступ**: Sudo привилегии обязательны

### Что вам понадобится:
1. Сервер с установленной Ubuntu/Debian
2. SSH доступ к серверу
3. API ключи от Binance (создайте на binance.com)
4. Базовые знания работы с командной строкой Linux

---

## Этап 1: Подготовка локальной машины

### 1.1. Клонирование репозитория

На вашей локальной машине выполните:

```bash
# Клонируйте репозиторий
git clone https://github.com/anthony87b7f58e-coder/life_is_a_joke.git

# Перейдите в директорию проекта
cd life_is_a_joke

# Переключитесь на нужную ветку
git checkout copilot/create-deployment-infrastructure-files

# Проверьте, что все файлы на месте
ls -la
```

Вы должны увидеть следующие директории:
- `src/` - код приложения
- `deployment/` - скрипты развертывания
- `scripts/` - утилиты
- `.env.template` - шаблон конфигурации

---

## Этап 2: Подключение к серверу и начальная настройка

### 2.1. Подключитесь к серверу по SSH

```bash
# Замените YOUR_SERVER_IP на IP вашего сервера
ssh root@YOUR_SERVER_IP

# Или если используете отдельного пользователя:
ssh your_username@YOUR_SERVER_IP
```

### 2.2. Обновите систему

После подключения к серверу выполните:

```bash
# Обновите список пакетов
sudo apt update

# Обновите установленные пакеты
sudo apt upgrade -y

# Установите базовые утилиты
sudo apt install -y git curl wget nano vim unzip
```

---

## Этап 3: Загрузка файлов на сервер

### Вариант А: Прямое клонирование на сервер (рекомендуется)

```bash
# На сервере создайте рабочую директорию
mkdir -p ~/trading-bot-setup
cd ~/trading-bot-setup

# Клонируйте репозиторий
git clone https://github.com/anthony87b7f58e-coder/life_is_a_joke.git

# Перейдите в директорию
cd life_is_a_joke

# Переключитесь на нужную ветку
git checkout copilot/create-deployment-infrastructure-files

# Проверьте содержимое
ls -la
```

### Вариант Б: Загрузка через SCP (если клонирование не работает)

На вашей локальной машине:

```bash
# Создайте архив проекта
cd life_is_a_joke
tar -czf trading-bot.tar.gz .

# Загрузите на сервер (замените YOUR_SERVER_IP)
scp trading-bot.tar.gz root@YOUR_SERVER_IP:/root/

# На сервере распакуйте
ssh root@YOUR_SERVER_IP
cd /root
mkdir -p trading-bot-setup
tar -xzf trading-bot.tar.gz -C trading-bot-setup/
cd trading-bot-setup
```

---

## Этап 4: Установка Python и зависимостей

### 4.1. Установите Python 3.8+

```bash
# Проверьте версию Python
python3 --version

# Если версия < 3.8, установите Python 3.10
sudo apt install -y python3.10 python3.10-venv python3-pip

# Установите дополнительные пакеты для Python
sudo apt install -y python3-dev build-essential
```

### 4.2. Проверьте установку

```bash
# Проверьте Python
python3 --version  # Должно быть >= 3.8

# Проверьте pip
pip3 --version
```

---

## Этап 5: Запуск автоматического развертывания

### 5.1. Сделайте скрипт развертывания исполняемым

```bash
# Перейдите в директорию с проектом
cd ~/trading-bot-setup/life_is_a_joke

# Сделайте скрипт исполняемым
chmod +x deployment/deploy.sh

# Проверьте права
ls -la deployment/deploy.sh
```

### 5.2. Запустите автоматическое развертывание

```bash
# ВАЖНО: Запускайте с sudo!
sudo bash deployment/deploy.sh
```

**Что делает этот скрипт:**
1. ✅ Проверяет системные требования
2. ✅ Создает пользователя `tradingbot`
3. ✅ Создает необходимые директории:
   - `/opt/trading-bot` - приложение
   - `/etc/trading-bot` - конфигурация
   - `/var/lib/trading-bot` - база данных
   - `/var/log/trading-bot` - логи
   - `/var/backups/trading-bot` - бэкапы
4. ✅ Устанавливает системные зависимости (nginx, supervisor, etc.)
5. ✅ Создает Python virtual environment
6. ✅ Устанавливает Python зависимости
7. ✅ Копирует файлы приложения
8. ✅ Устанавливает systemd сервис
9. ✅ Настраивает nginx (опционально)
10. ✅ Настраивает логротацию

**Процесс займет 5-10 минут.**

### 5.3. Проверьте результаты установки

```bash
# Проверьте, что пользователь создан
id tradingbot

# Проверьте директории
ls -la /opt/trading-bot
ls -la /etc/trading-bot
ls -la /var/lib/trading-bot
ls -la /var/log/trading-bot

# Проверьте systemd сервис
systemctl status trading-bot
```

---

## Этап 6: Настройка конфигурации бота

### 6.1. Интерактивная настройка (рекомендуется)

```bash
# Запустите интерактивный конфигуратор
sudo python3 /opt/trading-bot/scripts/setup_environment.py
```

Следуйте инструкциям на экране:

1. **Базовая конфигурация:**
   - Application name: `trading-bot` (нажмите Enter)
   - Environment: `production` (нажмите Enter)
   - Enable debug mode: `n`
   - Log level: `INFO` (нажмите Enter)

2. **Binance API:**
   - Binance API Key: `введите ваш ключ`
   - Binance API Secret: `введите ваш секрет`
   - Use Binance Testnet: `y` (для первого запуска!)

3. **База данных:**
   - Select database type: `1` (SQLite)
   - Database path: `/var/lib/trading-bot/trading_bot.db` (Enter)

4. **Настройки торговли:**
   - Enable live trading: `n` (для первого запуска!)
   - Default trading symbol: `BTCUSDT` (Enter)
   - Maximum position size: `0.1` (Enter)
   - Stop loss percentage: `2.0` (Enter)
   - Take profit percentage: `5.0` (Enter)

5. **Управление рисками:**
   - Maximum daily trades: `10` (Enter)
   - Maximum open positions: `3` (Enter)
   - Maximum daily loss percentage: `5.0` (Enter)
   - Position size percentage: `2.0` (Enter)

6. **Уведомления (опционально):**
   - Enable notifications: `n` (можно настроить позже)

### 6.2. Ручная настройка (альтернатива)

```bash
# Скопируйте шаблон конфигурации
sudo cp /opt/trading-bot/.env.template /etc/trading-bot/.env

# Отредактируйте конфигурацию
sudo nano /etc/trading-bot/.env
```

**Минимальная конфигурация для запуска:**

```bash
# Обязательные параметры
BINANCE_API_KEY=ваш_api_ключ_здесь
BINANCE_API_SECRET=ваш_api_секрет_здесь
BINANCE_TESTNET=true  # ВАЖНО: true для тестирования!

# Режим работы (для безопасного старта)
TRADING_ENABLED=false  # false = только мониторинг
APP_ENV=production
DEBUG=false
LOG_LEVEL=INFO

# База данных
DB_TYPE=sqlite
DB_PATH=/var/lib/trading-bot/trading_bot.db

# Торговые параметры
DEFAULT_SYMBOL=BTCUSDT
MAX_POSITION_SIZE=0.1
STOP_LOSS_PERCENTAGE=2.0
TAKE_PROFIT_PERCENTAGE=5.0

# Управление рисками
MAX_DAILY_TRADES=10
MAX_OPEN_POSITIONS=3
MAX_DAILY_LOSS_PERCENTAGE=5.0
POSITION_SIZE_PERCENTAGE=2.0
```

Сохраните файл: `Ctrl+X`, затем `Y`, затем `Enter`

### 6.3. Установите правильные права доступа

```bash
# Установите владельца
sudo chown tradingbot:tradingbot /etc/trading-bot/.env

# Установите права (только владелец может читать)
sudo chmod 600 /etc/trading-bot/.env

# Проверьте
ls -la /etc/trading-bot/.env
```

---

## Этап 7: Проверка подключения к Binance

### 7.1. Тест подключения к API

```bash
# Запустите тест подключения
cd /opt/trading-bot
sudo -u tradingbot python3 scripts/test_connectivity.py
```

**Ожидаемый результат:**
```
[INFO] Loading environment from: /etc/trading-bot/.env
[SUCCESS] Environment variables loaded
[INFO] === Testing Basic Connectivity ===
[SUCCESS] Internet connectivity: OK
[INFO] === Testing Binance Public API ===
[INFO] Using Binance TESTNET
[SUCCESS] Server time: 2024-12-27 10:30:45
[SUCCESS] Exchange info: 500 trading pairs available
[SUCCESS] BTCUSDT price: 45000.50
[INFO] === Testing Binance Authenticated API ===
[SUCCESS] Account status: OK
```

Если видите ошибки - проверьте API ключи в `/etc/trading-bot/.env`

---

## Этап 8: Проверка здоровья системы

### 8.1. Запустите health check

```bash
cd /opt/trading-bot
sudo python3 scripts/health_check.py
```

**Ожидаемый результат:**
```
======================================================================
Trading Bot - System Health Check
======================================================================
✓ CPU Usage: 15%
✓ Memory Usage: 35%
✓ Disk Usage: 25%
✓ APP_DIR: /opt/trading-bot (readable/writable)
✓ DATA_DIR: /var/lib/trading-bot (readable/writable)
✓ LOG_DIR: /var/log/trading-bot (readable/writable)
✓ Config: BINANCE_API_KEY: Set
✓ SQLite Database: /var/lib/trading-bot/trading_bot.db (0.00MB)
✓ Internet Connectivity: OK
======================================================================
System Status: HEALTHY
======================================================================
```

---

## Этап 9: Настройка безопасности (ОБЯЗАТЕЛЬНО!)

### 9.1. Настройте firewall

```bash
# Запустите скрипт настройки firewall
sudo bash /opt/trading-bot/deployment/scripts/setup_firewall.sh
```

Вас спросят:
- "Do you want to reset UFW to defaults?" - Нажмите `yes`
- "Do you want to enable the firewall now?" - Нажмите `yes`

**Что настраивается:**
- ✅ Закрываются все входящие порты
- ✅ Открывается SSH (порт 22)
- ✅ Настраивается rate limiting для SSH
- ✅ Опционально: HTTP/HTTPS если включен веб-интерфейс

### 9.2. Примените security hardening

```bash
# Запустите скрипт усиления безопасности
sudo bash /opt/trading-bot/deployment/scripts/security_hardening.sh
```

Вас спросят: "Do you want to continue?" - Нажмите `yes`

**Что делается:**
- ✅ Обновление системы
- ✅ Автоматические обновления безопасности
- ✅ SSH hardening (отключение root login, etc.)
- ✅ Настройка fail2ban
- ✅ Усиление параметров ядра
- ✅ Настройка audit logging

**ВАЖНО:** После этого скрипта перезагрузите SSH сервис:
```bash
sudo systemctl restart sshd
```

**ВНИМАНИЕ:** НЕ закрывайте текущую SSH сессию! Откройте новую в другом окне и проверьте, что SSH работает, прежде чем закрыть старую.

---

## Этап 10: Настройка автоматических бэкапов

### 10.1. Настройте cron для бэкапов

```bash
# Запустите скрипт настройки cron
sudo bash /opt/trading-bot/deployment/scripts/setup_cron.sh
```

Выберите расписание:
```
Select backup schedule:
  1) Daily at 2:00 AM (default)
  2) Every 6 hours
  3) Weekly on Sunday at midnight
  4) Monthly on the 1st at midnight
  5) Custom cron expression

Select option (1-5) [1]: 1
```

Нажмите `1` для ежедневных бэкапов в 2:00

Подтвердите: `Do you want to continue with this schedule? (yes/no):` - напишите `yes`

### 10.2. Проверьте настройку

```bash
# Проверьте crontab
sudo crontab -l | grep trading-bot

# Запустите тестовый бэкап вручную
sudo bash /opt/trading-bot/deployment/scripts/backup.sh
```

**Проверьте результат:**
```bash
ls -lh /var/backups/trading-bot/
```

Вы должны увидеть файл вида: `trading-bot_backup_20241227_103045.tar.gz`

---

## Этап 11: Запуск торгового бота

### 11.1. Режим мониторинга (безопасный старт)

**Перед запуском убедитесь в конфигурации:**

```bash
# Проверьте критические параметры
sudo grep -E "TRADING_ENABLED|BINANCE_TESTNET" /etc/trading-bot/.env
```

Должно быть:
```
TRADING_ENABLED=false
BINANCE_TESTNET=true
```

### 11.2. Запустите сервис

```bash
# Запустите бот
sudo systemctl start trading-bot

# Проверьте статус
sudo systemctl status trading-bot
```

**Ожидаемый результат:**
```
● trading-bot.service - Trading Bot Service
   Loaded: loaded (/etc/systemd/system/trading-bot.service; enabled)
   Active: active (running) since ...
```

### 11.3. Мониторинг логов в реальном времени

```bash
# Смотрите системные логи
sudo journalctl -u trading-bot -f

# Или смотрите файлы логов
sudo tail -f /var/log/trading-bot/trading-bot.log
```

**Что вы должны увидеть:**
```
2024-12-27 10:35:00 - __main__ - INFO - ======================================================================
2024-12-27 10:35:00 - __main__ - INFO - Trading Bot - Starting
2024-12-27 10:35:00 - __main__ - INFO - ======================================================================
2024-12-27 10:35:01 - __main__ - INFO - Configuration loaded: trading-bot
2024-12-27 10:35:01 - __main__ - INFO - Environment: production
2024-12-27 10:35:01 - __main__ - INFO - Trading enabled: False
2024-12-27 10:35:02 - bot - INFO - Connected to Binance TESTNET
2024-12-27 10:35:02 - bot - INFO - TRADING BOT STARTED
2024-12-27 10:35:02 - bot - INFO - Trading enabled: False
```

### 11.4. Остановка бота (если нужно)

```bash
# Остановите бот
sudo systemctl stop trading-bot

# Перезапустите
sudo systemctl restart trading-bot
```

---

## Этап 12: Включение автозапуска

### 12.1. Настройте автозапуск при старте системы

```bash
# Включите автозапуск
sudo systemctl enable trading-bot

# Проверьте
systemctl is-enabled trading-bot
```

Должно вывести: `enabled`

---

## Этап 13: Переход в режим тестовой торговли

После того как убедились, что бот работает в режиме мониторинга (несколько часов/дней):

### 13.1. Остановите бота

```bash
sudo systemctl stop trading-bot
```

### 13.2. Включите тестовую торговлю

```bash
# Отредактируйте конфигурацию
sudo nano /etc/trading-bot/.env

# Измените:
TRADING_ENABLED=true
BINANCE_TESTNET=true  # Оставьте true!
```

Сохраните: `Ctrl+X`, `Y`, `Enter`

### 13.3. Перезапустите бота

```bash
sudo systemctl start trading-bot
sudo journalctl -u trading-bot -f
```

Теперь бот будет реально торговать, но на testnet (виртуальные деньги).

---

## Этап 14: Переход к реальной торговле (ТОЛЬКО ПОСЛЕ ТЕСТОВ!)

**⚠️ ВНИМАНИЕ: Делайте это ТОЛЬКО после успешного тестирования на testnet!**

### 14.1. Создайте НОВЫЕ API ключи для production

На Binance:
1. Зайдите в API Management
2. Создайте новый API ключ
3. **НЕ давайте права на вывод средств!**
4. Включите только "Enable Spot & Margin Trading"
5. Добавьте IP сервера в whitelist (опционально, но рекомендуется)

### 14.2. Обновите конфигурацию

```bash
# Остановите бота
sudo systemctl stop trading-bot

# Создайте бэкап конфигурации
sudo cp /etc/trading-bot/.env /etc/trading-bot/.env.backup

# Отредактируйте
sudo nano /etc/trading-bot/.env
```

Измените:
```bash
BINANCE_API_KEY=ваш_НОВЫЙ_production_ключ
BINANCE_API_SECRET=ваш_НОВЫЙ_production_секрет
BINANCE_TESTNET=false  # Теперь false!
TRADING_ENABLED=true
```

### 14.3. Начните с малых сумм

```bash
# Также рекомендуется уменьшить размеры позиций
MAX_POSITION_SIZE=0.01  # Очень маленькая позиция для старта!
POSITION_SIZE_PERCENTAGE=1.0  # 1% вместо 2%
```

### 14.4. Запустите и мониторьте!

```bash
# Запустите
sudo systemctl start trading-bot

# ПОСТОЯННО следите за логами
sudo journalctl -u trading-bot -f
```

---

## Этап 15: Ежедневное обслуживание и мониторинг

### 15.1. Ежедневные проверки

```bash
# 1. Проверьте статус
sudo systemctl status trading-bot

# 2. Проверьте здоровье системы
cd /opt/trading-bot && sudo python3 scripts/health_check.py

# 3. Проверьте последние логи
sudo tail -100 /var/log/trading-bot/trading-bot.log

# 4. Проверьте базу данных
sudo sqlite3 /var/lib/trading-bot/trading_bot.db "SELECT COUNT(*) FROM trades;"
sudo sqlite3 /var/lib/trading-bot/trading_bot.db "SELECT * FROM positions WHERE status='open';"
```

### 15.2. Просмотр статистики торговли

```bash
# Сколько сделок сегодня
sudo sqlite3 /var/lib/trading-bot/trading_bot.db "SELECT COUNT(*) FROM trades WHERE DATE(timestamp) = DATE('now');"

# Прибыль/убыток за сегодня
sudo sqlite3 /var/lib/trading-bot/trading_bot.db "SELECT SUM(profit_loss) FROM trades WHERE DATE(timestamp) = DATE('now');"

# Последние 10 сделок
sudo sqlite3 /var/lib/trading-bot/trading_bot.db "SELECT * FROM trades ORDER BY timestamp DESC LIMIT 10;"
```

---

## Этап 16: Управление бэкапами

### 16.1. Создание бэкапа вручную

```bash
# Создайте бэкап
sudo bash /opt/trading-bot/deployment/scripts/backup.sh

# Проверьте
ls -lh /var/backups/trading-bot/
```

### 16.2. Восстановление из бэкапа

```bash
# Посмотрите доступные бэкапы
sudo bash /opt/trading-bot/deployment/scripts/restore.sh

# Выберите бэкап для восстановления
# Следуйте инструкциям на экране
```

---

## Этап 17: Обновление бота

### 17.1. При выходе новой версии

```bash
# 1. Остановите бота
sudo systemctl stop trading-bot

# 2. Создайте бэкап
sudo bash /opt/trading-bot/deployment/scripts/backup.sh

# 3. Перейдите в репозиторий
cd ~/trading-bot-setup/life_is_a_joke

# 4. Обновите код
git pull origin copilot/create-deployment-infrastructure-files

# 5. Скопируйте новые файлы
sudo cp -r src/* /opt/trading-bot/src/
sudo cp -r scripts/* /opt/trading-bot/scripts/

# 6. Обновите зависимости
sudo /opt/trading-bot/venv/bin/pip install -r requirements.txt

# 7. Перезапустите
sudo systemctl start trading-bot
```

---

## Этап 18: Решение проблем

### 18.1. Бот не запускается

```bash
# Проверьте логи ошибок
sudo journalctl -u trading-bot -xe

# Проверьте конфигурацию
sudo cat /etc/trading-bot/.env | grep -E "BINANCE|TRADING"

# Проверьте права
ls -la /etc/trading-bot/.env
ls -la /var/lib/trading-bot
ls -la /var/log/trading-bot
```

### 18.2. Ошибки подключения к API

```bash
# Проверьте подключение к Binance
cd /opt/trading-bot
sudo -u tradingbot python3 scripts/test_connectivity.py

# Проверьте firewall
sudo ufw status
```

### 18.3. Проблемы с базой данных

```bash
# Проверьте файл БД
ls -la /var/lib/trading-bot/trading_bot.db

# Попробуйте открыть
sudo sqlite3 /var/lib/trading-bot/trading_bot.db ".tables"

# Если повреждена - восстановите из бэкапа
sudo bash /opt/trading-bot/deployment/scripts/restore.sh
```

### 18.4. Высокая нагрузка CPU/RAM

```bash
# Проверьте использование ресурсов
top
htop

# Если бот потребляет много ресурсов - увеличьте интервал проверки
# Отредактируйте src/core/bot.py и измените time.sleep(60) на time.sleep(300)
```

---

## Этап 19: Полезные команды

### Управление сервисом
```bash
sudo systemctl start trading-bot      # Запуск
sudo systemctl stop trading-bot       # Остановка
sudo systemctl restart trading-bot    # Перезапуск
sudo systemctl status trading-bot     # Статус
sudo systemctl enable trading-bot     # Автозапуск ВКЛ
sudo systemctl disable trading-bot    # Автозапуск ВЫКЛ
```

### Просмотр логов
```bash
sudo journalctl -u trading-bot -f              # Системные логи (live)
sudo journalctl -u trading-bot --since today   # Логи за сегодня
sudo tail -f /var/log/trading-bot/trading-bot.log   # Файловые логи
sudo tail -100 /var/log/trading-bot/trading-bot.log # Последние 100 строк
```

### Работа с базой данных
```bash
# Открыть БД
sudo sqlite3 /var/lib/trading-bot/trading_bot.db

# Внутри sqlite3:
.tables                           # Список таблиц
SELECT * FROM trades LIMIT 10;    # Последние сделки
SELECT * FROM positions;          # Открытые позиции
.quit                             # Выход
```

### Бэкапы
```bash
sudo bash /opt/trading-bot/deployment/scripts/backup.sh    # Создать бэкап
sudo bash /opt/trading-bot/deployment/scripts/restore.sh   # Восстановить
ls -lh /var/backups/trading-bot/                          # Список бэкапов
```

---

## Этап 20: Чек-лист перед запуском в production

**Убедитесь, что выполнены ВСЕ пункты:**

- [ ] ✅ Сервер обновлен: `sudo apt update && sudo apt upgrade`
- [ ] ✅ Все файлы на месте: `/opt/trading-bot/*`
- [ ] ✅ Конфигурация настроена: `/etc/trading-bot/.env`
- [ ] ✅ API ключи валидны (протестированы)
- [ ] ✅ Firewall настроен: `sudo ufw status`
- [ ] ✅ Security hardening применен
- [ ] ✅ SSH работает корректно после hardening
- [ ] ✅ Бэкапы настроены: `sudo crontab -l`
- [ ] ✅ Тестовый бэкап создан и работает
- [ ] ✅ Бот протестирован в режиме мониторинга (1+ день)
- [ ] ✅ Бот протестирован на testnet (3+ дня)
- [ ] ✅ Автозапуск включен: `systemctl is-enabled trading-bot`
- [ ] ✅ Health check проходит успешно
- [ ] ✅ Логи мониторятся
- [ ] ✅ Начальный капитал МИНИМАЛЕН (для тестов)
- [ ] ✅ Position size установлен МИНИМАЛЬНЫМ
- [ ] ✅ У вас есть план действий при ошибках
- [ ] ✅ Вы понимаете, как остановить бота

---

## 🎯 Краткая шпаргалка

### Первый запуск (на одной странице):

```bash
# 1. Подключитесь к серверу
ssh root@YOUR_SERVER_IP

# 2. Клонируйте репозиторий
cd ~ && git clone https://github.com/anthony87b7f58e-coder/life_is_a_joke.git
cd life_is_a_joke
git checkout copilot/create-deployment-infrastructure-files

# 3. Запустите установку
sudo bash deployment/deploy.sh

# 4. Настройте конфигурацию
sudo python3 /opt/trading-bot/scripts/setup_environment.py

# 5. Настройте безопасность
sudo bash /opt/trading-bot/deployment/scripts/setup_firewall.sh
sudo bash /opt/trading-bot/deployment/scripts/security_hardening.sh

# 6. Настройте бэкапы
sudo bash /opt/trading-bot/deployment/scripts/setup_cron.sh

# 7. Запустите бота
sudo systemctl start trading-bot
sudo systemctl enable trading-bot

# 8. Мониторьте
sudo journalctl -u trading-bot -f
```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `sudo journalctl -u trading-bot -xe`
2. Проверьте health check: `sudo python3 /opt/trading-bot/scripts/health_check.py`
3. Проверьте конфигурацию: `sudo cat /etc/trading-bot/.env`
4. Посмотрите README.md и PROJECT_OVERVIEW.md в репозитории

---

## ⚠️ ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ

1. **НЕ торгуйте реальными деньгами без тестирования!**
2. **ВСЕГДА начинайте с режима мониторинга**
3. **ВСЕГДА тестируйте на testnet перед production**
4. **НИКОГДА не давайте API ключам права на вывод средств**
5. **Регулярно проверяйте логи и позиции**
6. **Имейте план действий при сбоях**
7. **Начинайте с МИНИМАЛЬНЫХ сумм**
8. **Криптовалютная торговля - это высокий риск!**

---

## ✅ Установка завершена!

Поздравляем! Ваш торговый бот установлен и готов к работе.

**Следующие шаги:**
1. Мониторьте работу бота в течение нескольких дней
2. Изучите логи и убедитесь в корректности работы
3. Постепенно увеличивайте размеры позиций
4. Регулярно проверяйте бэкапы
5. Следите за обновлениями репозитория

**Удачной торговли! 🚀**
